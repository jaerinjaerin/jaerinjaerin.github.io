---
title: '등수 구하기'
description: '이중 for문을 활용한 등수 계산 알고리즘'
date: '2025-10-02'
tags: ['Algorithm']
featured: false
thumbnail: '/thumbnails/js-algorithm.png'
---

import { Steps, Step } from 'fumadocs-ui/components/steps';
import { Callout } from 'fumadocs-ui/components/callout';

## 문제

`N(1<=N<=100)`명의 학생의 국어점수가 입력되면 각 학생의 등수를 입력된 순서대로 출력하는 프로그램을 작성하세요.

### 입력설명

첫 줄에 `N(3<=N<=1000)`이 입력되고, 두 번째 줄에 국어점수를 의미하는 N개의 정수가 입력된다.

같은 점수가 입력될 경우 높은 등수로 동일 처리한다. 즉 가장 높은 점수가 92점인데 92점이 3명 존재하면 1등이 3명이고 그 다음 학생은 4등이 된다.

### 출력설명

입력된 순서대로 등수를 출력한다.

### 입력예제

```
5
87 89 92 100 76
```

### 출력예제

```
4 3 2 1 5
```

---

## Solution

<Steps>

<Step>

### 등수 배열 초기화

등수를 보여줄 배열을 만든다. 이 배열의 크기는 arr.length이다.

```tsx
const 등수가_적힌_배열 = Array.from({ length: arr.length }, () => 1); // [1, 1, 1, 1]
```

</Step>

<Step>

### 이중 for문으로 탐색

이중 for문으로 요소를 탐색한다.

```tsx
for (let i = 0; i < arr.length; i++) {
  for (let j = 0; j < arr.length; j++) {}
}
```

<Callout type='info'>i가 기준이 되고 j가 전체 배열의 요소를 순회한다.</Callout>

</Step>

<Step>

### 등수 증가 조건

이때 arr[i]가 arr[j]보다 작으면 `등수가_적힌_배열[i]`의 값을 증가시킨다.

```tsx
for (let i = 0; i < arr.length; i++) {
  for (let j = 0; j < arr.length; j++) {
    if (arr[i] < arr[j]) 등수가_적힌_배열[i]++;
  }
}
```

</Step>

<Step>

### 결과 반환

반복문이 종료되면 `등수가_적힌_배열`을 리턴한다.

</Step>

</Steps>

---

## 회고

<Callout type="success">
**알고리즘의 활용**

나중에 순위를 정해야하는 프로그램에 유용하게 사용할 알고리즘인것 같습니다.

</Callout>

<Callout type="warn">
**문제 해결 능력의 차이**

문제를 해결하는 방법을 빠르게 캐치하지 못해서 시간을 많이 쓴 문제가 되어버렸습니다. 기능구현의 문제해결능력과 코딩테스트의 문제해결능력은 다른걸까요? 공부 정말 열심히 해야겠다!라는 생각이 들었습니다.

</Callout>
